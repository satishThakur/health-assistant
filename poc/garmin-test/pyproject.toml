[project]
name = "garmin-poc"
version = "0.1.0"
description = "POC to test python-garminconnect library"
requires-python = ">=3.11"
dependencies = [
    "garminconnect>=0.2.19",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
